#!/bin/bash
if [ -p /dev/stdin ]; then
	while read  line; do
		echo "- $(date -I) - ${line}" >> $INBOX_DOC
		notify-send "Added \"${line}\" to inbox"
        done
else
	echo "- $(date -I) - ${1}" >> $INBOX_DOC
	notify-send "Added \"${1}\" to inbox"
fi
