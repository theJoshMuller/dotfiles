#!/bin/sh

if [ -p /dev/stdin ]; then
        while IFS= read line; do
		sed -i "/ === Next Actions ===/a- ${line}" $MASTER_DOC
        done
else
	sed -i "/ === Next Actions ===/a- ${1}" $MASTER_DOC
fi
